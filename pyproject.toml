[project]
name = "gemini-ai-agent"
version = "0.1.0"
description = ""
authors = [
    {name = "Shreya Porwal",email = "porwalshreya11111@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10,<4.0"
dependencies = [
    "google-adk (>=1.19.0,<2.0.0)",
    "timezonefinder (>=8.1.0,<9.0.0)",
    "geopy (>=2.4.1,<3.0.0)",
    "python-dotenv (>=1.2.1,<2.0.0)"
]

[tool.poetry]
packages = [{include = "gemini_ai_agent"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
